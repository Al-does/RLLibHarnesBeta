[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "rl-harness"
version = "0.1.0"
description = "Shared RLlib research library (harness, learners, losses, envs, analysis, devops)"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.12"
dependencies = [
    "ray[rllib]==2.56.0",
    "torch>=2.9,<3",
    "matplotlib>=3.9",
    "scipy>=1.18.0",
]

[project.optional-dependencies]
storage = [
    "boto3>=1.35",
]

[project.scripts]
rl-harness = "harness.cli:main"
rlh-flash = "devops.flash.provision:main"

[tool.setuptools.packages.find]
where = ["."]
include = [
    "analysis*",
    "devops*",
    "envs*",
    "harness*",
    "learners*",
    "losses*",
]
exclude = [
    "envs.cassandra_machine.tests*",
    "envs.hmm.tests*",
    "envs.mess3.tests*",
    "envs.wing.tests*",
]
namespaces = true

[tool.pytest.ini_options]
markers = ["slow: long-running tests"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
# Local Mac dev tooling for renting/bootstrapping vast.ai boxes. Kept out of the
# training env (never installed on the boxes) — run via `uv run --group devops`.
devops = [
    "vastai>=0.2.0",
]
flash = [
    "runpod-flash",
]
